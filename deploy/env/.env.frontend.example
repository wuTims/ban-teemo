# =============================================================================
# Frontend Environment Variables (Vite)
# =============================================================================
# Copy this file to frontend/.env.local for local development
# For production (Cloudflare), set these in the Pages dashboard
#
# IMPORTANT: Only variables prefixed with VITE_ are exposed to the browser

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# Backend API URL
# Local development:
VITE_API_URL=http://localhost:8000

# Production (update with your Render URL):
# VITE_API_URL=https://ban-teemo-api.onrender.com
